[project]
name = "runtime-status"
version = "0.1.0"
description = "Thread-safe runtime progress and exit reporting"
authors = [{ name = "Your Name" }]
dependencies = []
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"